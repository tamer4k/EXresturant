@model IEnumerable<EXresturant.Models.Menukaart>





<div class="whiteBackground" border>
    <div class="row">
        <div class="col-6">
            <h2 class="text-info">Menukaart List</h2>
        </div>
        <div class="col-6 text-right">
            <partial name="CreateButtonPartial" />
        </div>
        @if (Model.Count() == 0)
        {
            <div class="col-6">
                <h2 class="text-danger">Er is nog geen item</h2>
            </div>
            <div class="col-6 text-right">

            </div>
        }
        else
        {
            <div class="col-12">
                <table id="table_id" class="table table-striped table-dark border" style="background-color:#1e3144">
                    <thead>
                        <tr style=" background-color: #dc3545;">
                            <th>
                                @Html.DisplayNameFor(m => m.Id)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Naam)
                            </th>


                            <th></th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="text-warning font-weight-bold">
                                    @Html.DisplayFor(m => item.Id)
                                </td>
                                <td class="text-white-50 font-weight-bold">
                                    @Html.DisplayFor(modelItem => item.Naam)
                                </td>
                                <td style="width:150px">
                                    <partial name="OperationsButtonsPartial" model="item.Id" />
                                </td>
                                <td style="width:150px">
                                    <a asp-controller="Categories" asp-action="Index" class="btn btn-warning text-dark border-white ml-1" asp-route-id="@item.Id">
                                        <i class="fas fa-clipboard-list"></i>
                                    </a>
                                </td>

                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }

    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('#table_id').DataTable({
                "lengthMenu": [[4, 25, 50, -1], [4, 25, 50, "All"]]
            });
        });

    </script>
}
